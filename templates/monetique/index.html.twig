{% extends 'base.html.twig' %}

{% block title %}Monetique index{% endblock %}

{% block body %}
    <h1>Monetique index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Label</th>
                <th>Code</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for monetique in monetiques %}
            <tr>
                <td>{{ monetique.id }}</td>
                <td>{{ monetique.label }}</td>
                <td>{{ monetique.code }}</td>
                <td>
                    <a href="">show</a>
                    <a href="{{ path('app_monetique_edit', {'id': monetique.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_monetique_new') }}">Create new</a>
{% endblock %}
