{% extends 'base.html.twig' %}

{% block title %}FormStepChamps{% endblock %}

{% block body %}
    <h1>FormStepChamps</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_step_champ.id }}</td>
            </tr>
            <tr>
                <th>Ordre</th>
                <td>{{ form_step_champ.ordre }}</td>
            </tr>
            <tr>
                <th>Actif</th>
                <td>{{ form_step_champ.actif ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Obligatoire</th>
                <td>{{ form_step_champ.obligatoire ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ form_step_champ.createdAt ? form_step_champ.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ form_step_champ.updatedAt ? form_step_champ.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_form_step_champs_index') }}">back to list</a>

    <a href="{{ path('app_form_step_champs_edit', {'id': form_step_champ.id}) }}">edit</a>

    {{ include('form_step_champs/_delete_form.html.twig') }}
{% endblock %}
