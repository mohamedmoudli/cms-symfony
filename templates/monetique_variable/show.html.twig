{% extends 'base.html.twig' %}

{% block title %}MonetiqueVariable{% endblock %}

{% block body %}
    <h1>MonetiqueVariable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ monetique_variable.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ monetique_variable.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_monetique_variable_index') }}">back to list</a>

    <a href="{{ path('app_monetique_variable_edit', {'id': monetique_variable.id}) }}">edit</a>

    {{ include('monetique_variable/_delete_form.html.twig') }}
{% endblock %}
