{% extends 'base.html.twig' %}

{% block title %}FormEtat{% endblock %}

{% block body %}
    <h1>FormEtat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_etat.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ form_etat.label }}</td>
            </tr>
            <tr>
                <th>Code_html</th>
                <td>{{ form_etat.codeHtml }}</td>
            </tr>
            <tr>
                <th>Code_css</th>
                <td>{{ form_etat.codeCss }}</td>
            </tr>
            <tr>
                <th>Code_compile_html</th>
                <td>{{ form_etat.codeCompileHtml }}</td>
            </tr>
            <tr>
                <th>CodeCompileCss</th>
                <td>{{ form_etat.codeCompileCss }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ form_etat.createdAt ? form_etat.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ form_etat.updatedAt ? form_etat.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_form_etat_index') }}">back to list</a>

    <a href="{{ path('app_form_etat_edit', {'id': form_etat.id}) }}">edit</a>

    {{ include('form_etat/_delete_form.html.twig') }}
{% endblock %}
