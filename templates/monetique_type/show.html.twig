{% extends 'base.html.twig' %}

{% block title %}MonetiqueType{% endblock %}

{% block body %}
    <h1>MonetiqueType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ monetique_type.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ monetique_type.label }}</td>
            </tr>
            <tr>
                <th>Variable</th>
                <td>{{ monetique_type.variable }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_monetique_type_index') }}">back to list</a>

    <a href="{{ path('app_monetique_type_edit', {'id': monetique_type.id}) }}">edit</a>

    {{ include('monetique_type/_delete_form.html.twig') }}
{% endblock %}
