<form method="post" action="{{ path('app_form_champs_option_delete', {'id': form_champs_option.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ form_champs_option.id) }}">
    <button class="btn">Delete</button>
</form>
